# Local app data / tooling internals
.freebuff/
.hermes/
cleanup/

# Database files (SQLite)
*.db
*.db-shm
*.db-wal

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/
node_modules/

# Game file backups (apex_line.html.bak.*) and scratch
# validation/search scripts (_chk.js, _grep.js, _scope.js etc.)
# generated during iteration; never meant to ship.
*.bak*
_*.js

# Tooling-internal docs (Zenmux env key manager — unrelated to the game)
ZENMUX_HERMES_GUIDE.md

# Earlier experimental builds pre-dating apex_line.html, and the
# dev-cycle sample text notes. Kept locally for reference but never
# shipped to the game repo.
drift_racer_2d_ui_patched.html
sample*.txt
